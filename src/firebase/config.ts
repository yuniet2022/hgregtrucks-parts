// ==========================================
// FIREBASE REMOVED FROM FRONTEND FOR SECURITY
// 
// No credentials, no sensitive data in frontend code.
// The frontend uses localStorage only (no external services).
// 
// When you're ready to add a real backend server,
// use tRPC + MySQL with backend-building skill instead.
// Credentials stay on the server, never reach the browser.
// ==========================================

export const isConfigured = false;
export const db = null;
export const auth = null;
export const functions = null;
export default null;
